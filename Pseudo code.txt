// Pseudo code:

for(int i = 0; i < m_renderTargets.size(); ++i)
{
	context->OMSetRenderTargets(i, m_renderTargets[i], nullptr);
	context->ClearRenderTargetView(m_renderTargets[i], ....);
	for(int j = 0; j < scenes.nodes.size(); ++i)
	{
		scenes.nodes[j]->Render();
	}
	// set quad properties and draw.
}

swapChain->Present(0, 0);